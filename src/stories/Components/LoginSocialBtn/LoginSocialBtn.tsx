import React from "react";
import style from "./LoginSocialBtn.module.scss";
import "../../../index.css";

type Props = {
  
};

const LoginSocialBtn = (props: Props) => {
  return (
    <div className={`${style.LoginSocialBtn}`}>
     <h1>asdasd</h1>
    </div>
  );
};

export default LoginSocialBtn;
